package publicapi

import (
	"context"
	"database/sql"
	"net/http"
	"net/http/httptest"
	"regexp"
	"slices"
	"testing"
	"time"

	"github.com/DATA-DOG/go-sqlmock"
	"github.com/google/uuid"

	"github.com/publira/publira/server/internal/ageverification"
	dbmodels "github.com/publira/publira/server/internal/db/gen"
	"github.com/publira/publira/server/internal/emailsettings"
	publirattypesv1 "github.com/publira/publira/server/internal/proto/gen/publira/types/v1"
	"github.com/publira/publira/server/internal/storage"
	"github.com/publira/publira/server/internal/testutil"
)

const (
	getPublishedSeriesIDByPublicIDQuery           = "-- name: GetPublishedSeriesIDByPublicID :one\n"
	getTenantByIDQuery                            = "-- name: GetTenantByID :one\nSELECT id, public_id, domain, name, default_reading_period_hours, created_at, status, admin_domain, timezone, default_locale\nFROM tenants\nWHERE id = $1\nLIMIT 1\n"
	listActiveSeriesIDsByPublishedAtDescQuery     = "-- name: ListActiveSeriesIDsByPublishedAtDesc :many\nSELECT s.id\nFROM series s\nWHERE s.tenant_id = $1\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        NOT $2::boolean\n        OR EXISTS (\n            SELECT 1\n            FROM published_free_episodes fe\n            WHERE fe.series_id = s.id\n        )\n    )\n    AND (\n        $3::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_genres sg\n                JOIN genres g ON g.id = sg.genre_id\n            WHERE sg.tenant_id = $1\n                AND sg.series_id = s.id\n                AND g.public_id = $3::text\n        )\n    )\n    AND (\n        $4::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_tags st\n                JOIN tags t ON t.id = st.tag_id\n            WHERE st.tenant_id = $1\n                AND st.series_id = s.id\n                AND t.slug = $4::text\n        )\n    )\n    AND (\n        $5::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_listings sl\n            WHERE sl.tenant_id = $1\n                AND sl.series_id = s.id\n                AND sl.status = $5::text\n        )\n    )\n    AND (\n        $6::int2 IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_listings sl\n            WHERE sl.tenant_id = $1\n                AND sl.series_id = s.id\n                AND sl.schedule_weekdays @> ARRAY[$6::int2]\n        )\n    )\n    AND (\n        $7::uuid IS NULL\n        OR (\n            $8::boolean\n            AND (s.published_at, s.id) <= (\n                $9::timestamptz,\n                $7::uuid\n            )\n        )\n        OR (\n            NOT $8::boolean\n            AND (s.published_at, s.id) < (\n                $9::timestamptz,\n                $7::uuid\n            )\n        )\n    )\nORDER BY s.published_at DESC,\n    s.id DESC\nLIMIT $10\n"
	listActiveSeriesIDsByPublishedAtAscQuery      = "-- name: ListActiveSeriesIDsByPublishedAtAsc :many\nSELECT s.id\nFROM series s\nWHERE s.tenant_id = $1\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        NOT $2::boolean\n        OR EXISTS (\n            SELECT 1\n            FROM published_free_episodes fe\n            WHERE fe.series_id = s.id\n        )\n    )\n    AND (\n        $3::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_genres sg\n                JOIN genres g ON g.id = sg.genre_id\n            WHERE sg.tenant_id = $1\n                AND sg.series_id = s.id\n                AND g.public_id = $3::text\n        )\n    )\n    AND (\n        $4::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_tags st\n                JOIN tags t ON t.id = st.tag_id\n            WHERE st.tenant_id = $1\n                AND st.series_id = s.id\n                AND t.slug = $4::text\n        )\n    )\n    AND (\n        $5::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_listings sl\n            WHERE sl.tenant_id = $1\n                AND sl.series_id = s.id\n                AND sl.status = $5::text\n        )\n    )\n    AND (\n        $6::int2 IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_listings sl\n            WHERE sl.tenant_id = $1\n                AND sl.series_id = s.id\n                AND sl.schedule_weekdays @> ARRAY[$6::int2]\n        )\n    )\n    AND (\n        $7::uuid IS NULL\n        OR (\n            $8::boolean\n            AND (s.published_at, s.id) >= (\n                $9::timestamptz,\n                $7::uuid\n            )\n        )\n        OR (\n            NOT $8::boolean\n            AND (s.published_at, s.id) > (\n                $9::timestamptz,\n                $7::uuid\n            )\n        )\n    )\nORDER BY s.published_at ASC,\n    s.id ASC\nLIMIT $10\n"
	listActiveSeriesIDsByTitleAscQuery            = "-- name: ListActiveSeriesIDsByTitleAsc :many\nSELECT s.id\nFROM series s\nWHERE s.tenant_id = $1\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        NOT $2::boolean\n        OR EXISTS (\n            SELECT 1\n            FROM published_free_episodes fe\n            WHERE fe.series_id = s.id\n        )\n    )\n    AND (\n        $3::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_genres sg\n                JOIN genres g ON g.id = sg.genre_id\n            WHERE sg.tenant_id = $1\n                AND sg.series_id = s.id\n                AND g.public_id = $3::text\n        )\n    )\n    AND (\n        $4::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_tags st\n                JOIN tags t ON t.id = st.tag_id\n            WHERE st.tenant_id = $1\n                AND st.series_id = s.id\n                AND t.slug = $4::text\n        )\n    )\n    AND (\n        $5::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_listings sl\n            WHERE sl.tenant_id = $1\n                AND sl.series_id = s.id\n                AND sl.status = $5::text\n        )\n    )\n    AND (\n        $6::int2 IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_listings sl\n            WHERE sl.tenant_id = $1\n                AND sl.series_id = s.id\n                AND sl.schedule_weekdays @> ARRAY[$6::int2]\n        )\n    )\n    AND (\n        $7::uuid IS NULL\n        OR (\n            $8::boolean\n            AND (s.title, s.id) >= (\n                $9::text,\n                $7::uuid\n            )\n        )\n        OR (\n            NOT $8::boolean\n            AND (s.title, s.id) > (\n                $9::text,\n                $7::uuid\n            )\n        )\n    )\nORDER BY s.title ASC,\n    s.id ASC\nLIMIT $10\n"
	listActiveSeriesIDsByTitleDescQuery           = "-- name: ListActiveSeriesIDsByTitleDesc :many\nSELECT s.id\nFROM series s\nWHERE s.tenant_id = $1\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        NOT $2::boolean\n        OR EXISTS (\n            SELECT 1\n            FROM published_free_episodes fe\n            WHERE fe.series_id = s.id\n        )\n    )\n    AND (\n        $3::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_genres sg\n                JOIN genres g ON g.id = sg.genre_id\n            WHERE sg.tenant_id = $1\n                AND sg.series_id = s.id\n                AND g.public_id = $3::text\n        )\n    )\n    AND (\n        $4::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_tags st\n                JOIN tags t ON t.id = st.tag_id\n            WHERE st.tenant_id = $1\n                AND st.series_id = s.id\n                AND t.slug = $4::text\n        )\n    )\n    AND (\n        $5::text IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_listings sl\n            WHERE sl.tenant_id = $1\n                AND sl.series_id = s.id\n                AND sl.status = $5::text\n        )\n    )\n    AND (\n        $6::int2 IS NULL\n        OR EXISTS (\n            SELECT 1\n            FROM series_listings sl\n            WHERE sl.tenant_id = $1\n                AND sl.series_id = s.id\n                AND sl.schedule_weekdays @> ARRAY[$6::int2]\n        )\n    )\n    AND (\n        $7::uuid IS NULL\n        OR (\n            $8::boolean\n            AND (s.title, s.id) <= (\n                $9::text,\n                $7::uuid\n            )\n        )\n        OR (\n            NOT $8::boolean\n            AND (s.title, s.id) < (\n                $9::text,\n                $7::uuid\n            )\n        )\n    )\nORDER BY s.title DESC,\n    s.id DESC\nLIMIT $10\n"
	listActiveSeriesByIDsQuery                    = "-- name: ListActiveSeriesByIDs :many\nSELECT s.id,\n    s.public_id,\n    s.title,\n    sl.synopsis,\n    sl.status,\n    sl.schedule_weekdays,\n    sl.age_rating,\n    s.published_at,\n    s.eye_catch_image_id,\n    NULL::timestamp AS eye_catch_image_updated_at,\n    (\n        SELECT COUNT(*)\n        FROM published_free_episodes fe\n        WHERE fe.series_id = s.id\n    )::int4 AS free_episode_count,\n    COALESCE(\n        json_agg(\n            json_build_object(\n                'public_id',\n                c.public_id,\n                'name',\n                c.name,\n                'role_public_id',\n                cr.public_id,\n                'role_name',\n                cr.name,\n                'profile_text',\n                c.profile_text,\n                'icon_image_url',\n                CASE\n                    WHEN c.icon_image_id IS NOT NULL THEN '/images/creators/' || c.icon_image_id::text\n                    ELSE ''\n                END,\n                'icon_image_file_size_bytes',\n                0,\n                'icon_image_updated_at',\n                COALESCE(ci.updated_at::TEXT, '')\n            )\n            -- Role priority first, so the leading role opens the list. A\n            -- credit written before roles existed states none and comes last.\n            ORDER BY cr.display_priority ASC NULLS LAST,\n                sc.display_order ASC,\n                c.name ASC\n        ) FILTER (\n            WHERE c.id IS NOT NULL\n        ),\n        '[]'\n    )::jsonb AS creators,\n    -- The classification rides on this row rather than on queries of its own,\n    -- so a page costs the round trips it did before series carried genres and\n    -- tags. Both are sub-selects rather than joins because the row is already\n    -- grouped for the creators, and a second and third join would multiply\n    -- what that aggregate counts.\n    COALESCE(\n        (\n            SELECT json_agg(\n                    json_build_object(\n                        'public_id',\n                        g.public_id,\n                        'name',\n                        g.name,\n                        'slug',\n                        g.slug\n                    )\n                    -- The tenant's own genre order rather than the order the\n                    -- series was assigned them in, so every series presents\n                    -- them the way the genre list does.\n                    ORDER BY g.display_order ASC,\n                        g.id ASC\n                )\n            FROM series_genres sg\n                JOIN genres g ON g.id = sg.genre_id\n            WHERE sg.tenant_id = $1\n                AND sg.series_id = s.id\n        ),\n        '[]'\n    )::jsonb AS genres,\n    COALESCE(\n        (\n            SELECT json_agg(\n                    json_build_object(\n                        'name',\n                        t.name,\n                        'slug',\n                        t.slug\n                    )\n                    -- Tags have no order of their own, so they read back by\n                    -- name: the same series shows the same list every time.\n                    ORDER BY t.name ASC,\n                        t.id ASC\n                )\n            FROM series_tags st\n                JOIN tags t ON t.id = st.tag_id\n            WHERE st.tenant_id = $1\n                AND st.series_id = s.id\n        ),\n        '[]'\n    )::jsonb AS tags,\n    CASE\n        WHEN l.public_id IS NOT NULL THEN json_build_object(\n            'public_id',\n            l.public_id,\n            'name',\n            l.name\n        )\n        ELSE '{}'::json\n    END::jsonb AS label_info\nFROM series s\n    LEFT JOIN series_listings sl ON sl.series_id = s.id\n    LEFT JOIN labels l ON s.label_id = l.id\n    LEFT JOIN series_creators sc ON s.id = sc.series_id\n    LEFT JOIN creators c ON sc.creator_id = c.id\n    LEFT JOIN creator_roles cr ON cr.id = sc.role_id\n    LEFT JOIN creator_images ci ON ci.id = c.icon_image_id\nWHERE s.tenant_id = $1\n    AND s.id = ANY($2::uuid [])\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\nGROUP BY s.id,\n    sl.series_id,\n    sl.synopsis,\n    sl.status,\n    sl.schedule_weekdays,\n    sl.age_rating,\n    l.public_id,\n    l.name\n"
	getSeriesDetailQuery                          = "-- name: GetSeriesDetail :one\nSELECT s.id,\n    s.public_id,\n    s.title,\n    l.public_id AS label_public_id,\n    l.name AS label_name,\n    s.eye_catch_image_id,\n    NULL::timestamp AS eye_catch_image_updated_at,\n    sl.synopsis,\n    sl.status,\n    sl.schedule_weekdays,\n    sl.age_rating,\n    -- The series' own comment mode, and NULL when it follows the tenant's. The\n    -- caller resolves the two, so this read carries the override rather than\n    -- the answer.\n    sl.comment_mode,\n    s.is_published,\n    s.published_at,\n    (\n        SELECT COUNT(*)\n        FROM published_free_episodes fe\n        WHERE fe.series_id = s.id\n    )::int4 AS free_episode_count,\n    -- Collect the several creators into one column as a JSON array\n    COALESCE(\n        json_agg(\n            json_build_object(\n                    'public_id',\n                    c.public_id,\n                'name',\n                c.name,\n                    'role_public_id',\n                    cr.public_id,\n                    'role_name',\n                    cr.name,\n                    'profile_text',\n                    c.profile_text,\n                    'icon_image_url',\n                    CASE\n                        WHEN c.icon_image_id IS NOT NULL THEN '/images/creators/' || c.icon_image_id::text\n                        ELSE ''\n                    END,\n                    'icon_image_file_size_bytes',\n                    0,\n                    'icon_image_updated_at',\n                    COALESCE(ci.updated_at::TEXT, '')\n            )\n            -- Role priority first, so the leading role opens the list. A\n            -- credit written before roles existed states none and comes last.\n            ORDER BY cr.display_priority ASC NULLS LAST,\n                sc.display_order ASC,\n                c.name ASC\n        ) FILTER (\n            WHERE c.id IS NOT NULL\n        ),\n        '[]'\n    )::jsonb AS creators,\n    -- The classification the list carries, so a detail page states the same\n    -- genres and tags a card did. Sub-selects rather than joins, because this\n    -- row is already grouped for the creators.\n    COALESCE(\n        (\n            SELECT json_agg(\n                    json_build_object(\n                        'public_id',\n                        g.public_id,\n                        'name',\n                        g.name,\n                        'slug',\n                        g.slug\n                    )\n                    ORDER BY g.display_order ASC,\n                        g.id ASC\n                )\n            FROM series_genres sg\n                JOIN genres g ON g.id = sg.genre_id\n            WHERE sg.series_id = s.id\n        ),\n        '[]'\n    )::jsonb AS genres,\n    COALESCE(\n        (\n            SELECT json_agg(\n                    json_build_object(\n                        'name',\n                        t.name,\n                        'slug',\n                        t.slug\n                    )\n                    ORDER BY t.name ASC,\n                        t.id ASC\n                )\n            FROM series_tags st\n                JOIN tags t ON t.id = st.tag_id\n            WHERE st.series_id = s.id\n        ),\n        '[]'\n    )::jsonb AS tags,\n    COALESCE(\n        (\n            SELECT json_agg(\n                    json_build_object(\n                        'public_id',\n                        e.public_id,\n                        'title',\n                        e.title,\n                        'order_index',\n                        e.order_index,\n                        'price',\n                        el.price,\n                        'reading_period_hours',\n                        el.reading_period_hours,\n                        'status',\n                        el.status,\n                        'scheduled_at',\n                        el.scheduled_at,\n                        'published_at',\n                        el.published_at\n                    )\n                    -- order_index can tie, so the UUIDv7 id is the\n                    -- tiebreaker that keeps the order unique. It is the order\n                    -- the episode detail's neighbour links are found in, and\n                    -- the two have to agree: a reader following \"next\" from\n                    -- this list must land where the list said they would.\n                    ORDER BY e.order_index ASC,\n                        e.id ASC\n                )\n            FROM episodes e\n                JOIN episode_listings el ON el.episode_id = e.id\n            WHERE e.series_id = s.id\n                AND el.status = 'published'\n                AND el.published_at IS NOT NULL\n                AND el.published_at <= NOW()\n        ),\n        '[]'\n    )::jsonb AS episodes\nFROM series s\n    LEFT JOIN series_listings sl ON sl.series_id = s.id\n    LEFT JOIN labels l ON s.label_id = l.id\n    LEFT JOIN series_creators sc ON s.id = sc.series_id\n    LEFT JOIN creators c ON sc.creator_id = c.id\n    LEFT JOIN creator_roles cr ON cr.id = sc.role_id\n    LEFT JOIN creator_images ci ON ci.id = c.icon_image_id\nWHERE s.public_id = $1\n    AND s.tenant_id = $2\nGROUP BY s.id,\n    l.id,\n    sl.series_id,\n    sl.synopsis,\n    sl.status,\n    sl.schedule_weekdays,\n    sl.age_rating,\n    sl.comment_mode\n"
	listSeriesImageVariantsByImageIDsQuery        = "-- name: ListSeriesImageVariantsByImageIDs :many\nSELECT series_image_id,\n    variant_type,\n    label,\n    content_type,\n    file_size_bytes,\n    width,\n    height\nFROM series_image_variants\nWHERE series_image_id = ANY($1::uuid[])\nORDER BY series_image_id,\n    variant_type,\n    width\n"
	listEpisodeCreatorsByEpisodeIDsQuery          = "-- name: ListEpisodeCreatorsByEpisodeIDs :many\nSELECT ec.episode_id,\n    c.public_id,\n    c.name,\n    c.profile_text,\n    c.icon_image_id,\n    ci.updated_at AS icon_image_updated_at,\n    cr.public_id AS role_public_id,\n    cr.name AS role_name,\n    ec.display_order,\n    ec.source\nFROM episode_creators ec\n    JOIN creators c ON c.id = ec.creator_id\n    LEFT JOIN creator_roles cr ON cr.id = ec.role_id\n    LEFT JOIN creator_images ci ON ci.id = c.icon_image_id\nWHERE ec.episode_id = ANY($1::uuid[])\nORDER BY ec.episode_id ASC,\n    cr.display_priority ASC NULLS LAST,\n    ec.display_order ASC,\n    c.name ASC\n"
	listPublishedEpisodeNeighborsQuery            = "-- name: ListPublishedEpisodeNeighborsForTenant :many\n(\n    SELECT -1::int4 AS direction,\n        e.id,\n        e.public_id,\n        e.title,\n        e.order_index,\n        el.price,\n        (\n            el.price = 0\n            OR EXISTS (\n                SELECT 1\n                FROM episode_free_windows fw\n                WHERE fw.episode_id = e.id\n                    AND fw.starts_at <= NOW()\n                    AND fw.ends_at > NOW()\n            )\n        ) AS is_free\n    FROM episodes e\n        JOIN series s ON s.id = e.series_id\n        JOIN episode_listings el ON el.episode_id = e.id\n    WHERE s.tenant_id = $1\n        AND e.series_id = $2\n        AND (e.order_index, e.id) < ($3::int4, $4::uuid)\n        AND s.is_published = true\n        AND s.published_at IS NOT NULL\n        AND s.published_at <= NOW()\n        AND el.status = 'published'\n        AND el.published_at IS NOT NULL\n        AND el.published_at <= NOW()\n    ORDER BY e.order_index DESC,\n        e.id DESC\n    LIMIT 1\n)\nUNION ALL\n(\n    SELECT 1::int4 AS direction,\n        e.id,\n        e.public_id,\n        e.title,\n        e.order_index,\n        el.price,\n        (\n            el.price = 0\n            OR EXISTS (\n                SELECT 1\n                FROM episode_free_windows fw\n                WHERE fw.episode_id = e.id\n                    AND fw.starts_at <= NOW()\n                    AND fw.ends_at > NOW()\n            )\n        ) AS is_free\n    FROM episodes e\n        JOIN series s ON s.id = e.series_id\n        JOIN episode_listings el ON el.episode_id = e.id\n    WHERE s.tenant_id = $1\n        AND e.series_id = $2\n        AND (e.order_index, e.id) > ($3::int4, $4::uuid)\n        AND s.is_published = true\n        AND s.published_at IS NOT NULL\n        AND s.published_at <= NOW()\n        AND el.status = 'published'\n        AND el.published_at IS NOT NULL\n        AND el.published_at <= NOW()\n    ORDER BY e.order_index ASC,\n        e.id ASC\n    LIMIT 1\n)\n"
	getPublishedEpisodeByPublicIDQuery            = "-- name: GetPublishedEpisodeByPublicIDForTenant :one\nSELECT e.id,\n    e.public_id,\n    e.title,\n    e.order_index,\n    e.series_id,\n    el.price,\n    el.reading_period_hours,\n    el.status,\n    el.scheduled_at,\n    el.published_at,\n    s.public_id AS series_public_id,\n    s.title AS series_title,\n    -- The rating a client interposes its confirmation on. Reading it here\n    -- keeps the episode detail one round trip.\n    sl.age_rating AS series_age_rating,\n    -- The series' own comment mode, and NULL when it follows the tenant's.\n    -- Posting resolves the two, and reads the episode either way, so the\n    -- override travels with the episode rather than costing a query of its own.\n    sl.comment_mode AS series_comment_mode,\n    -- The end of the free window covering this instant, or NULL when none\n    -- does. Windows on one episode cannot overlap, so at most one row answers.\n    -- A priced episode inside one reads as free until this moment, which is\n    -- also what the response shows the reader as a countdown.\n    fw.ends_at AS free_until,\n    -- How many readers have rated this episode. The stored tally, so the join\n    -- is one row rather than a scan of the ratings, and an episode nobody has\n    -- rated has no row at all and reads as 0.\n    COALESCE(erc.count, 0)::bigint AS rating_count\nFROM episodes e\n    JOIN series s ON s.id = e.series_id\n    JOIN episode_listings el ON el.episode_id = e.id\n    LEFT JOIN series_listings sl ON sl.series_id = s.id\n    -- At most one window can cover an instant of an episode, so this join\n    -- cannot multiply the row.\n    LEFT JOIN episode_free_windows fw ON fw.episode_id = e.id\n    AND fw.starts_at <= NOW()\n    AND fw.ends_at > NOW()\n    LEFT JOIN episode_rating_counts erc ON erc.tenant_id = s.tenant_id\n    AND erc.episode_id = e.id\nWHERE s.tenant_id = $1\n    AND e.public_id = $2\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND el.status = 'published'\n    AND el.published_at IS NOT NULL\n    AND el.published_at <= NOW()\nLIMIT 1\n"
	listPublishedPagesForTenantQuery              = "-- name: ListPublishedPagesForTenant :many\nSELECT p.id, p.tenant_id, p.slug, p.title, p.published_version_id, p.display_in_footer, p.created_at, p.updated_at\nFROM pages p\n\tJOIN page_versions pv ON pv.id = p.published_version_id\nWHERE p.tenant_id = $1\n\tAND p.display_in_footer = true\n\tAND pv.status = 'published'\n\tAND pv.published_at IS NOT NULL\n\tAND pv.published_at <= NOW()\nORDER BY p.created_at ASC\n"
	getPublishedPageBySlugQuery                   = "-- name: GetPublishedPageBySlugForTenant :one\nSELECT p.id,\n\tp.tenant_id,\n\tp.slug,\n\tp.title,\n\tp.published_version_id,\n\tp.display_in_footer,\n\tp.created_at,\n\tp.updated_at,\n\tpv.id AS version_id,\n\tpv.page_id,\n\tpv.version_number,\n\tpv.content_markdown,\n\tpv.author_user_id,\n\tpv.status,\n\tpv.publish_at,\n\tpv.created_at AS version_created_at,\n\tpv.published_at\nFROM pages p\n\tJOIN page_versions pv ON pv.id = p.published_version_id\nWHERE p.tenant_id = $1\n\tAND p.slug = $2\n\tAND pv.status = 'published'\n\tAND pv.published_at IS NOT NULL\n\tAND pv.published_at <= NOW()\nLIMIT 1\n"
	listEpisodeImagesByEpisodeIDQuery             = "-- name: ListEpisodeImagesByEpisodeID :many\nSELECT\n    ei.id,\n    ei.tenant_id,\n    ei.episode_id,\n    ei.display_order,\n    ei.created_at,\n    eiv.content_type,\n    eiv.file_size_bytes,\n    eiv.width,\n    eiv.height\nFROM episode_images ei\nJOIN LATERAL (\n    SELECT content_type, file_size_bytes, width, height\n    FROM episode_image_variants\n    WHERE episode_image_id = ei.id\n    ORDER BY width DESC\n    LIMIT 1\n) eiv ON true\nWHERE ei.episode_id = $1\nORDER BY ei.display_order ASC,\n    ei.created_at ASC\n"
	insertDebouncedEpisodeViewEventQuery          = "-- name: InsertDebouncedEpisodeViewEvent :one\nINSERT INTO content_events (\n    id,\n    tenant_id,\n    event_type,\n    user_id,\n    anonymous_id,\n    series_id,\n    episode_id,\n    debounce_bucket,\n    payload,\n    occurred_at\n) VALUES (\n    $1,\n    $2,\n    'episode_view',\n    $3,\n    $4,\n    $5::uuid,\n    $6::uuid,\n    $7::bigint,\n    $8,\n    $9\n)\nON CONFLICT (tenant_id, event_type, episode_id, actor_key, debounce_bucket)\nWHERE event_type = 'episode_view'\nDO NOTHING\nRETURNING id, tenant_id, event_type, user_id, anonymous_id, actor_key, series_id, episode_id, debounce_bucket, rating_score, source_table, source_id, payload, occurred_at, created_at\n"
	insertDebouncedSeriesViewEventQuery           = "-- name: InsertDebouncedSeriesViewEvent :one\nINSERT INTO content_events (\n    id,\n    tenant_id,\n    event_type,\n    user_id,\n    anonymous_id,\n    series_id,\n    debounce_bucket,\n    payload,\n    occurred_at\n) VALUES (\n    $1,\n    $2,\n    'series_view',\n    $3,\n    $4,\n    $5::uuid,\n    $6::bigint,\n    $7,\n    $8\n)\nON CONFLICT (tenant_id, event_type, series_id, actor_key, debounce_bucket)\nWHERE event_type = 'series_view'\nDO NOTHING\nRETURNING id, tenant_id, event_type, user_id, anonymous_id, actor_key, series_id, episode_id, debounce_bucket, rating_score, source_table, source_id, payload, occurred_at, created_at\n"
	userHasEpisodeContentAccessQuery              = "-- name: UserHasEpisodeContentAccess :one\nSELECT (\n        EXISTS (\n            SELECT 1\n            FROM purchases p\n            WHERE p.tenant_id = $1\n                -- The cast keeps this a plain uuid: a deleted buyer's NULL is nobody's grant.\n                AND p.user_id = $2::uuid\n                AND p.episode_id = $3\n                AND (\n                    p.expires_at IS NULL\n                    OR p.expires_at > NOW()\n                )\n                AND p.refunded_at IS NULL\n        )\n        OR EXISTS (\n            SELECT 1\n            FROM access_tickets at\n            WHERE at.tenant_id = $1\n                AND at.user_id = $2\n                AND at.episode_id = $3\n                AND at.revoked_at IS NULL\n                AND (\n                    at.expires_at IS NULL\n                    OR at.expires_at > NOW()\n                )\n        )\n    ) AS has_access\n"
	listPublishedCreatorIDsByNameAscQuery         = "-- name: ListPublishedCreatorIDsByNameAsc :many\nSELECT c.id\nFROM creators c\nWHERE c.tenant_id = $1\n    AND EXISTS (\n        SELECT 1\n        FROM series_creators sc\n            JOIN series s ON s.id = sc.series_id\n        WHERE sc.creator_id = c.id\n            AND s.tenant_id = c.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    )\n    AND (\n        $2::uuid IS NULL\n        OR (\n            $3::boolean\n            AND (c.name, c.id) >= (\n                $4::text,\n                $2::uuid\n            )\n        )\n        OR (\n            NOT $3::boolean\n            AND (c.name, c.id) > (\n                $4::text,\n                $2::uuid\n            )\n        )\n    )\nORDER BY c.name ASC,\n    c.id ASC\nLIMIT $5\n"
	listPublishedCreatorIDsByNameDescQuery        = "-- name: ListPublishedCreatorIDsByNameDesc :many\nSELECT c.id\nFROM creators c\nWHERE c.tenant_id = $1\n    AND EXISTS (\n        SELECT 1\n        FROM series_creators sc\n            JOIN series s ON s.id = sc.series_id\n        WHERE sc.creator_id = c.id\n            AND s.tenant_id = c.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    )\n    AND (\n        $2::uuid IS NULL\n        OR (\n            $3::boolean\n            AND (c.name, c.id) <= (\n                $4::text,\n                $2::uuid\n            )\n        )\n        OR (\n            NOT $3::boolean\n            AND (c.name, c.id) < (\n                $4::text,\n                $2::uuid\n            )\n        )\n    )\nORDER BY c.name DESC,\n    c.id DESC\nLIMIT $5\n"
	listPublishedCreatorsByIDsQuery               = "-- name: ListPublishedCreatorsByIDs :many\nSELECT c.id,\n    c.public_id,\n    c.name,\n    c.profile_text,\n    c.icon_image_id,\n    ci.updated_at AS icon_image_updated_at,\n    COALESCE(civ.file_size_bytes, 0)::bigint AS icon_image_file_size_bytes,\n    (\n        SELECT COUNT(*)::int4\n        FROM series_creators sc\n            JOIN series s ON s.id = sc.series_id\n        WHERE sc.creator_id = c.id\n            AND s.tenant_id = c.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    ) AS published_series_count\nFROM creators c\n    LEFT JOIN creator_images ci ON ci.id = c.icon_image_id\n    LEFT JOIN LATERAL (\n        SELECT file_size_bytes\n        FROM creator_image_variants\n        WHERE creator_image_id = ci.id\n        ORDER BY width DESC\n        LIMIT 1\n    ) civ ON true\nWHERE c.tenant_id = $1\n    AND c.id = ANY($2::uuid [])\n"
	getPublishedCreatorByPublicIDQuery            = "-- name: GetPublishedCreatorByPublicID :one\nSELECT c.id,\n    c.public_id,\n    c.name,\n    c.profile_text,\n    c.icon_image_id,\n    ci.updated_at AS icon_image_updated_at,\n    COALESCE(civ.file_size_bytes, 0)::bigint AS icon_image_file_size_bytes,\n    (\n        SELECT COUNT(*)::int4\n        FROM series_creators sc\n            JOIN series s ON s.id = sc.series_id\n        WHERE sc.creator_id = c.id\n            AND s.tenant_id = c.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    ) AS published_series_count\nFROM creators c\n    LEFT JOIN creator_images ci ON ci.id = c.icon_image_id\n    LEFT JOIN LATERAL (\n        SELECT file_size_bytes\n        FROM creator_image_variants\n        WHERE creator_image_id = ci.id\n        ORDER BY width DESC\n        LIMIT 1\n    ) civ ON true\nWHERE c.tenant_id = $1\n    AND c.public_id = $2\n    AND EXISTS (\n        SELECT 1\n        FROM series_creators sc\n            JOIN series s ON s.id = sc.series_id\n        WHERE sc.creator_id = c.id\n            AND s.tenant_id = c.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    )\nLIMIT 1\n"
	listPublishedSeriesIDsByCreatorTitleAscQuery  = "-- name: ListPublishedSeriesIDsByCreatorTitleAsc :many\nSELECT s.id\nFROM series s\n    JOIN series_creators sc ON sc.series_id = s.id\nWHERE sc.creator_id = $1\n    AND s.tenant_id = $2\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (s.title, s.id) >= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (s.title, s.id) > (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY s.title ASC,\n    s.id ASC\nLIMIT $6\n"
	listPublishedSeriesIDsByCreatorTitleDescQuery = "-- name: ListPublishedSeriesIDsByCreatorTitleDesc :many\nSELECT s.id\nFROM series s\n    JOIN series_creators sc ON sc.series_id = s.id\nWHERE sc.creator_id = $1\n    AND s.tenant_id = $2\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (s.title, s.id) <= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (s.title, s.id) < (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY s.title DESC,\n    s.id DESC\nLIMIT $6\n"
	getPublishedLabelByPublicIDQuery              = "-- name: GetPublishedLabelByPublicID :one\nSELECT l.id,\n    l.public_id,\n    l.name,\n    l.eye_catch_image_id,\n    li.updated_at AS eye_catch_image_updated_at,\n    (\n        SELECT COUNT(*)::int4\n        FROM series s\n        WHERE s.label_id = l.id\n            AND s.tenant_id = l.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    ) AS published_series_count\nFROM labels l\n    LEFT JOIN label_images li ON li.id = l.eye_catch_image_id\nWHERE l.tenant_id = $1\n    AND l.public_id = $2\nLIMIT 1\n"
	listPublishedSeriesIDsByLabelTitleAscQuery    = "-- name: ListPublishedSeriesIDsByLabelTitleAsc :many\nSELECT s.id\nFROM series s\nWHERE s.label_id = $1::uuid\n    AND s.tenant_id = $2\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (s.title, s.id) >= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (s.title, s.id) > (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY s.title ASC,\n    s.id ASC\nLIMIT $6\n"
	listPublishedSeriesIDsByLabelTitleDescQuery   = "-- name: ListPublishedSeriesIDsByLabelTitleDesc :many\nSELECT s.id\nFROM series s\nWHERE s.label_id = $1::uuid\n    AND s.tenant_id = $2\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (s.title, s.id) <= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (s.title, s.id) < (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY s.title DESC,\n    s.id DESC\nLIMIT $6\n"
	listPublishedSeriesIDsBySearchTitleAscQuery   = "-- name: ListPublishedSeriesIDsBySearchTitleAsc :many\nSELECT s.id\nFROM series s\n    LEFT JOIN series_listings sl ON sl.series_id = s.id\nWHERE s.tenant_id = $1\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        s.title ILIKE $2::text ESCAPE '!'\n        OR COALESCE(sl.synopsis, '') ILIKE $2::text ESCAPE '!'\n    )\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (s.title, s.id) >= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (s.title, s.id) > (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY s.title ASC,\n    s.id ASC\nLIMIT $6\n"
	listPublishedSeriesIDsBySearchTitleDescQuery  = "-- name: ListPublishedSeriesIDsBySearchTitleDesc :many\nSELECT s.id\nFROM series s\n    LEFT JOIN series_listings sl ON sl.series_id = s.id\nWHERE s.tenant_id = $1\n    AND s.is_published = true\n    AND s.published_at IS NOT NULL\n    AND s.published_at <= NOW()\n    AND (\n        s.title ILIKE $2::text ESCAPE '!'\n        OR COALESCE(sl.synopsis, '') ILIKE $2::text ESCAPE '!'\n    )\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (s.title, s.id) <= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (s.title, s.id) < (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY s.title DESC,\n    s.id DESC\nLIMIT $6\n"
	listActiveSeriesIDsByLatestEpisodeAtDescQuery = "-- name: ListActiveSeriesIDsByLatestEpisodeAtDesc :many\nWITH candidate AS (\n    SELECT s.id,\n        COALESCE(\n            (\n                SELECT max(el.published_at)\n                FROM episodes e\n                    JOIN episode_listings el ON el.episode_id = e.id\n                WHERE e.series_id = s.id\n                    AND el.status = 'published'\n                    AND el.published_at IS NOT NULL\n                    AND el.published_at <= NOW()\n            ),\n            s.published_at\n        )::timestamptz AS latest_episode_at\n    FROM series s\n    WHERE s.tenant_id = $5\n        AND s.is_published = true\n        AND s.published_at IS NOT NULL\n        AND s.published_at <= NOW()\n        AND (\n            NOT $6::boolean\n            OR EXISTS (\n                SELECT 1\n                FROM published_free_episodes fe\n                WHERE fe.series_id = s.id\n            )\n        )\n        AND (\n            $7::text IS NULL\n            OR EXISTS (\n                SELECT 1\n                FROM series_genres sg\n                    JOIN genres g ON g.id = sg.genre_id\n                WHERE sg.tenant_id = $5\n                    AND sg.series_id = s.id\n                    AND g.public_id = $7::text\n            )\n        )\n        AND (\n            $8::text IS NULL\n            OR EXISTS (\n                SELECT 1\n                FROM series_tags st\n                    JOIN tags t ON t.id = st.tag_id\n                WHERE st.tenant_id = $5\n                    AND st.series_id = s.id\n                    AND t.slug = $8::text\n            )\n        )\n        AND (\n            $9::text IS NULL\n            OR EXISTS (\n                SELECT 1\n                FROM series_listings sl\n                WHERE sl.tenant_id = $5\n                    AND sl.series_id = s.id\n                    AND sl.status = $9::text\n            )\n        )\n        AND (\n            $10::int2 IS NULL\n            OR EXISTS (\n                SELECT 1\n                FROM series_listings sl\n                WHERE sl.tenant_id = $5\n                    AND sl.series_id = s.id\n                    AND sl.schedule_weekdays @> ARRAY[$10::int2]\n            )\n        )\n)\nSELECT id,\n    latest_episode_at\nFROM candidate\nWHERE (\n        $1::uuid IS NULL\n        OR (\n            $2::boolean\n            AND (latest_episode_at, id) <= (\n                $3::timestamptz,\n                $1::uuid\n            )\n        )\n        OR (\n            NOT $2::boolean\n            AND (latest_episode_at, id) < (\n                $3::timestamptz,\n                $1::uuid\n            )\n        )\n    )\nORDER BY latest_episode_at DESC,\n    id DESC\nLIMIT $4\n"
	listActiveSeriesIDsByLatestEpisodeAtAscQuery  = "-- name: ListActiveSeriesIDsByLatestEpisodeAtAsc :many\nWITH candidate AS (\n    SELECT s.id,\n        COALESCE(\n            (\n                SELECT max(el.published_at)\n                FROM episodes e\n                    JOIN episode_listings el ON el.episode_id = e.id\n                WHERE e.series_id = s.id\n                    AND el.status = 'published'\n                    AND el.published_at IS NOT NULL\n                    AND el.published_at <= NOW()\n            ),\n            s.published_at\n        )::timestamptz AS latest_episode_at\n    FROM series s\n    WHERE s.tenant_id = $5\n        AND s.is_published = true\n        AND s.published_at IS NOT NULL\n        AND s.published_at <= NOW()\n        AND (\n            NOT $6::boolean\n            OR EXISTS (\n                SELECT 1\n                FROM published_free_episodes fe\n                WHERE fe.series_id = s.id\n            )\n        )\n        AND (\n            $7::text IS NULL\n            OR EXISTS (\n                SELECT 1\n                FROM series_genres sg\n                    JOIN genres g ON g.id = sg.genre_id\n                WHERE sg.tenant_id = $5\n                    AND sg.series_id = s.id\n                    AND g.public_id = $7::text\n            )\n        )\n        AND (\n            $8::text IS NULL\n            OR EXISTS (\n                SELECT 1\n                FROM series_tags st\n                    JOIN tags t ON t.id = st.tag_id\n                WHERE st.tenant_id = $5\n                    AND st.series_id = s.id\n                    AND t.slug = $8::text\n            )\n        )\n        AND (\n            $9::text IS NULL\n            OR EXISTS (\n                SELECT 1\n                FROM series_listings sl\n                WHERE sl.tenant_id = $5\n                    AND sl.series_id = s.id\n                    AND sl.status = $9::text\n            )\n        )\n        AND (\n            $10::int2 IS NULL\n            OR EXISTS (\n                SELECT 1\n                FROM series_listings sl\n                WHERE sl.tenant_id = $5\n                    AND sl.series_id = s.id\n                    AND sl.schedule_weekdays @> ARRAY[$10::int2]\n            )\n        )\n)\nSELECT id,\n    latest_episode_at\nFROM candidate\nWHERE (\n        $1::uuid IS NULL\n        OR (\n            $2::boolean\n            AND (latest_episode_at, id) >= (\n                $3::timestamptz,\n                $1::uuid\n            )\n        )\n        OR (\n            NOT $2::boolean\n            AND (latest_episode_at, id) > (\n                $3::timestamptz,\n                $1::uuid\n            )\n        )\n    )\nORDER BY latest_episode_at ASC,\n    id ASC\nLIMIT $4\n"
	getGenreIDByPublicIDForTenantQuery            = "-- name: GetGenreIDByPublicIDForTenant :one\nSELECT g.id\nFROM genres g\nWHERE g.tenant_id = $1\n    AND g.public_id = $2\nLIMIT 1\n"
	getTagBySlugForTenantQuery                    = "-- name: GetTagBySlugForTenant :one\nSELECT t.id\nFROM tags t\nWHERE t.tenant_id = $1\n    AND t.slug = $2\nLIMIT 1\n"
	listPublishedGenresByTenantAscQuery           = "-- name: ListPublishedGenresByTenantAsc :many\nSELECT g.id,\n    g.public_id,\n    g.name,\n    g.slug,\n    g.display_order,\n    (\n        SELECT COUNT(*)\n        FROM series_genres sg\n            JOIN series s ON s.id = sg.series_id\n        WHERE sg.tenant_id = $1\n            AND sg.genre_id = g.id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    )::int4 AS published_series_count\nFROM genres g\nWHERE g.tenant_id = $1\n    AND (\n        $2::uuid IS NULL\n        OR (\n            $3::boolean\n            AND (g.display_order, g.id) >= ($4::int4, $2::uuid)\n        )\n        OR (\n            NOT $3::boolean\n            AND (g.display_order, g.id) > ($4::int4, $2::uuid)\n        )\n    )\nORDER BY g.display_order ASC,\n    g.id ASC\nLIMIT $5\n"
	listPublishedGenresByTenantDescQuery          = "-- name: ListPublishedGenresByTenantDesc :many\nSELECT g.id,\n    g.public_id,\n    g.name,\n    g.slug,\n    g.display_order,\n    (\n        SELECT COUNT(*)\n        FROM series_genres sg\n            JOIN series s ON s.id = sg.series_id\n        WHERE sg.tenant_id = $1\n            AND sg.genre_id = g.id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    )::int4 AS published_series_count\nFROM genres g\nWHERE g.tenant_id = $1\n    AND (\n        $2::uuid IS NULL\n        OR (\n            $3::boolean\n            AND (g.display_order, g.id) <= ($4::int4, $2::uuid)\n        )\n        OR (\n            NOT $3::boolean\n            AND (g.display_order, g.id) < ($4::int4, $2::uuid)\n        )\n    )\nORDER BY g.display_order DESC,\n    g.id DESC\nLIMIT $5\n"
	listPublishedTagsByTenantDescQuery            = "-- name: ListPublishedTagsByTenantDesc :many\nWITH counted AS (\n    SELECT t.name,\n        t.slug,\n        COUNT(*)::int4 AS published_series_count\n    FROM tags t\n        JOIN series_tags st ON st.tag_id = t.id\n        JOIN series s ON s.id = st.series_id\n    WHERE t.tenant_id = $5\n        AND st.tenant_id = $5\n        AND s.is_published = true\n        AND s.published_at IS NOT NULL\n        AND s.published_at <= NOW()\n    GROUP BY t.id,\n        t.name,\n        t.slug\n)\nSELECT name,\n    slug,\n    published_series_count\nFROM counted\nWHERE (\n        $1::text IS NULL\n        OR published_series_count < $2::int4\n        OR (\n            published_series_count = $2::int4\n            AND (\n                (\n                    $3::boolean\n                    AND slug >= $1::text\n                )\n                OR (\n                    NOT $3::boolean\n                    AND slug > $1::text\n                )\n            )\n        )\n    )\nORDER BY published_series_count DESC,\n    slug ASC\nLIMIT $4\n"
	listPublishedTagsByTenantAscQuery             = "-- name: ListPublishedTagsByTenantAsc :many\nWITH counted AS (\n    SELECT t.name,\n        t.slug,\n        COUNT(*)::int4 AS published_series_count\n    FROM tags t\n        JOIN series_tags st ON st.tag_id = t.id\n        JOIN series s ON s.id = st.series_id\n    WHERE t.tenant_id = $5\n        AND st.tenant_id = $5\n        AND s.is_published = true\n        AND s.published_at IS NOT NULL\n        AND s.published_at <= NOW()\n    GROUP BY t.id,\n        t.name,\n        t.slug\n)\nSELECT name,\n    slug,\n    published_series_count\nFROM counted\nWHERE (\n        $1::text IS NULL\n        OR published_series_count > $2::int4\n        OR (\n            published_series_count = $2::int4\n            AND (\n                (\n                    $3::boolean\n                    AND slug <= $1::text\n                )\n                OR (\n                    NOT $3::boolean\n                    AND slug < $1::text\n                )\n            )\n        )\n    )\nORDER BY published_series_count ASC,\n    slug DESC\nLIMIT $4\n"
	listPublishedCreatorIDsBySearchNameAscQuery   = "-- name: ListPublishedCreatorIDsBySearchNameAsc :many\nSELECT c.id\nFROM creators c\nWHERE c.tenant_id = $1\n    AND c.name ILIKE $2::text ESCAPE '!'\n    AND EXISTS (\n        SELECT 1\n        FROM series_creators sc\n            JOIN series s ON s.id = sc.series_id\n        WHERE sc.creator_id = c.id\n            AND s.tenant_id = c.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    )\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (c.name, c.id) >= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (c.name, c.id) > (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY c.name ASC,\n    c.id ASC\nLIMIT $6\n"
	listPublishedCreatorIDsBySearchNameDescQuery  = "-- name: ListPublishedCreatorIDsBySearchNameDesc :many\nSELECT c.id\nFROM creators c\nWHERE c.tenant_id = $1\n    AND c.name ILIKE $2::text ESCAPE '!'\n    AND EXISTS (\n        SELECT 1\n        FROM series_creators sc\n            JOIN series s ON s.id = sc.series_id\n        WHERE sc.creator_id = c.id\n            AND s.tenant_id = c.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    )\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (c.name, c.id) <= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (c.name, c.id) < (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY c.name DESC,\n    c.id DESC\nLIMIT $6\n"
	listPublishedLabelsBySearchNameAscQuery       = "-- name: ListPublishedLabelsBySearchNameAsc :many\nSELECT l.id,\n    l.public_id,\n    l.name,\n    l.eye_catch_image_id,\n    li.updated_at AS eye_catch_image_updated_at\nFROM labels l\n    LEFT JOIN label_images li ON li.id = l.eye_catch_image_id\nWHERE l.tenant_id = $1\n    AND l.name ILIKE $2::text ESCAPE '!'\n    AND EXISTS (\n        SELECT 1\n        FROM series s\n        WHERE s.label_id = l.id\n            AND s.tenant_id = l.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    )\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (l.name, l.id) >= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (l.name, l.id) > (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY l.name ASC,\n    l.id ASC\nLIMIT $6\n"
	listPublishedLabelsBySearchNameDescQuery      = "-- name: ListPublishedLabelsBySearchNameDesc :many\nSELECT l.id,\n    l.public_id,\n    l.name,\n    l.eye_catch_image_id,\n    li.updated_at AS eye_catch_image_updated_at\nFROM labels l\n    LEFT JOIN label_images li ON li.id = l.eye_catch_image_id\nWHERE l.tenant_id = $1\n    AND l.name ILIKE $2::text ESCAPE '!'\n    AND EXISTS (\n        SELECT 1\n        FROM series s\n        WHERE s.label_id = l.id\n            AND s.tenant_id = l.tenant_id\n            AND s.is_published = true\n            AND s.published_at IS NOT NULL\n            AND s.published_at <= NOW()\n    )\n    AND (\n        $3::uuid IS NULL\n        OR (\n            $4::boolean\n            AND (l.name, l.id) <= (\n                $5::text,\n                $3::uuid\n            )\n        )\n        OR (\n            NOT $4::boolean\n            AND (l.name, l.id) < (\n                $5::text,\n                $3::uuid\n            )\n        )\n    )\nORDER BY l.name DESC,\n    l.id DESC\nLIMIT $6\n"
	listLabelImageVariantsByImageIDsQuery         = "-- name: ListLabelImageVariantsByImageIDs :many\nSELECT label_image_id,\n    variant_type,\n    label,\n    content_type,\n    file_size_bytes,\n    width,\n    height\nFROM label_image_variants\nWHERE label_image_id = ANY($1::uuid[])\nORDER BY label_image_id,\n    variant_type,\n    width\n"
)

func newTestPublicServer(t *testing.T) (*httptest.Server, sqlmock.Sqlmock) {
	t.Helper()
	db, mock, err := sqlmock.New()
	if err != nil {
		t.Fatalf("sqlmock.New: %v", err)
	}
	t.Cleanup(func() {
		_ = db.Close()
	})
	server := httptest.NewServer(mustPublicHandler(t, db, dbmodels.New(db), nil))
	t.Cleanup(server.Close)
	return server, mock
}

// mustPublicHandler builds the handler the way the server does, so a test that
// is about the routes or the wiring exercises the real constructor. It fails
// the test when the environment holds flood control settings the server would
// refuse to start on.
func mustPublicHandler(t *testing.T, db *sql.DB, queries Querier, encryptor emailsettings.SecretManager) http.Handler {
	t.Helper()

	handler, err := NewHandler(db, queries, &testStorageProvider{}, encryptor, testutil.TokenManager())
	if err != nil {
		t.Fatalf("NewHandler: %v", err)
	}
	return handler
}

type testStorageProvider struct{}

func (p *testStorageProvider) Upload(_ context.Context, req storage.UploadRequest) (storage.UploadResult, error) {
	return storage.UploadResult{
		Provider:  "s3",
		ObjectKey: req.ObjectKey,
		URL:       "s3://" + req.ObjectKey,
		SizeBytes: int64(len(req.Data)),
	}, nil
}

func publicTenantColumns() []string {
	return []string{"id", "public_id", "domain", "name", "default_reading_period_hours", "created_at", "status", "admin_domain", "timezone", "default_locale"}
}

// episodeNeighbor is one row of the neighbour read GetEpisodeDetail performs
// once it has the episode: direction -1 is the episode before it in the series,
// 1 the one after.
type episodeNeighbor struct {
	direction int32
	// id is what the credit read that follows asks about, so a neighbour that
	// is credited has to be identified here as well as named.
	id         uuid.UUID
	publicID   string
	title      string
	orderIndex int32
	price      int32
	// isFree is the query's own answer, price 0 or an open free window, rather
	// than something a test derives from the price beside it.
	isFree bool
}

// expectEpisodeNeighborsLookup registers that read. Passing no neighbour is an
// episode with none, which is what the query returns at both ends of a series.
func expectEpisodeNeighborsLookup(
	mock sqlmock.Sqlmock,
	tenantID uuid.UUID,
	seriesID uuid.UUID,
	orderIndex int32,
	episodeID uuid.UUID,
	neighbors ...episodeNeighbor,
) {
	rows := sqlmock.NewRows([]string{"direction", "id", "public_id", "title", "order_index", "price", "is_free"})
	for _, neighbor := range neighbors {
		rows.AddRow(neighbor.direction, neighbor.id, neighbor.publicID, neighbor.title, neighbor.orderIndex, neighbor.price, neighbor.isFree)
	}
	mock.ExpectQuery(regexp.QuoteMeta(listPublishedEpisodeNeighborsQuery)).
		WithArgs(tenantID, seriesID, orderIndex, episodeID).
		WillReturnRows(rows)
}

// episodeCredit is one row of the credit read GetEpisodeDetail performs for
// the episode it was asked about and its neighbours together.
type episodeCredit struct {
	episodeID    uuid.UUID
	publicID     string
	name         string
	rolePublicID string
	roleName     string
}

// expectEpisodeCreditsLookup registers that read. Passing no credit is an
// episode nobody is credited on, which is what an episode of a series with no
// credits bakes.
func expectEpisodeCreditsLookup(mock sqlmock.Sqlmock, credits ...episodeCredit) {
	rows := sqlmock.NewRows([]string{"episode_id", "public_id", "name", "profile_text", "icon_image_id", "icon_image_updated_at", "role_public_id", "role_name", "display_order", "source"})
	for index, credit := range credits {
		rows.AddRow(credit.episodeID, credit.publicID, credit.name, nil, nil, nil, credit.rolePublicID, credit.roleName, int32(index), "series")
	}
	mock.ExpectQuery(regexp.QuoteMeta(listEpisodeCreatorsByEpisodeIDsQuery)).
		WillReturnRows(rows)
}

func expectTenantLookup(mock sqlmock.Sqlmock, tenantID uuid.UUID, publicID string, now time.Time) {
	expectTenantLookupWithSettings(mock, tenantID, publicID, now, "Asia/Tokyo", "ja")
}

func expectTenantLookupWithTimezone(mock sqlmock.Sqlmock, tenantID uuid.UUID, publicID string, now time.Time, timezone string) {
	expectTenantLookupWithSettings(mock, tenantID, publicID, now, timezone, "ja")
}

func expectTenantLookupWithDefaultLocale(mock sqlmock.Sqlmock, tenantID uuid.UUID, publicID string, now time.Time, defaultLocale string) {
	expectTenantLookupWithSettings(mock, tenantID, publicID, now, "Asia/Tokyo", defaultLocale)
}

func expectTenantLookupWithSettings(mock sqlmock.Sqlmock, tenantID uuid.UUID, publicID string, now time.Time, timezone, defaultLocale string) {
	mock.ExpectQuery(regexp.QuoteMeta(getTenantByIDQuery)).
		WithArgs(tenantID).
		WillReturnRows(sqlmock.NewRows(publicTenantColumns()).
			AddRow(tenantID, publicID, "tenant.example", "Tenant", nil, now, "active", nil, timezone, defaultLocale))
}

const (
	getContentRankingSnapshotByIDQuery     = "-- name: GetContentRankingSnapshotByID :one\n"
	getLatestContentRankingSnapshotQuery   = "-- name: GetLatestContentRankingSnapshot :one\n"
	listLatestContentRankingSnapshotsQuery = "-- name: ListLatestContentRankingSnapshots :many\n"
	listRankedSeriesIDsQuery               = "-- name: ListRankedSeriesIDs :many\n"
	listRankedSeriesIDsReversedQuery       = "-- name: ListRankedSeriesIDsReversed :many\n"
	listRecommendedSeriesIDsQuery          = "-- name: ListRecommendedSeriesIDs :many\n"
	listRecommendedSeriesIDsReversedQuery  = "-- name: ListRecommendedSeriesIDsReversed :many\n"
	listRelatedSeriesIDsQuery              = "-- name: ListRelatedSeriesIDs :many\n"
	listRelatedSeriesIDsReversedQuery      = "-- name: ListRelatedSeriesIDsReversed :many\n"
)

// assertSeriesPublicIDs compares a series list against the public ids it should
// hold, in order. Order is the assertion in most of these cases, so a mismatch
// prints both lists rather than the first index that differs.
func assertSeriesPublicIDs(t *testing.T, items []*publirattypesv1.Series, want ...string) {
	t.Helper()

	got := seriesPublicIDs(items)
	if !slices.Equal(got, want) {
		t.Fatalf("series = %v, want %v", got, want)
	}
}

func contentRankingSnapshotColumns() []string {
	return []string{"id", "tenant_id", "ranking_key", "period_start", "period_end", "entity_type", "items", "algorithm_version", "computed_at"}
}

func contentEventColumns() []string {
	return []string{
		"id", "tenant_id", "event_type", "user_id", "anonymous_id", "actor_key",
		"series_id", "episode_id", "debounce_bucket", "rating_score",
		"source_table", "source_id", "payload", "occurred_at", "created_at",
	}
}

func assertPublicExpectations(t *testing.T, mock sqlmock.Sqlmock) {
	t.Helper()
	if err := mock.ExpectationsWereMet(); err != nil {
		t.Fatalf("unmet SQL expectations: %v", err)
	}
}

// tenantConfigColumns is the tenant_config row a `SELECT *` reads back.
func tenantConfigColumns() []string {
	return []string{
		"tenant_id", "copyright_text", "site_description", "created_at", "updated_at",
		"site_tagline", "comment_mode", "comment_auto_hide_report_threshold",
		"episode_rating_mode", "age_verification",
	}
}

// expectTenantConfigRead stands in for one read of the tenant's settings row.
// Two answers come out of it and each is read on its own, so a handler that
// needs both makes two of these.
func expectTenantConfigRead(mock sqlmock.Sqlmock, tenantID uuid.UUID, now time.Time, commentMode, ageRule string) {
	mock.ExpectQuery(regexp.QuoteMeta(getTenantConfigByTenantIDQuery)).
		WithArgs(tenantID).
		WillReturnRows(sqlmock.NewRows(tenantConfigColumns()).
			AddRow(tenantID, nil, nil, now, now, nil, commentMode, int32(3), "single", ageRule))
}

// expectTenantAgeVerification stands in for the tenant config read a rated
// series makes. Only a series that carries a rating causes it, so a test that
// sets none up is asserting the read never happened.
func expectTenantAgeVerification(mock sqlmock.Sqlmock, tenantID uuid.UUID, now time.Time, rule string) {
	expectTenantConfigRead(mock, tenantID, now, "disabled", rule)
}

// expectTenantCommentMode stands in for the tenant config read a series that
// states no comment mode of its own makes. A series carrying an override
// answers without it, so a test that sets none up is asserting the read never
// happened.
func expectTenantCommentMode(mock sqlmock.Sqlmock, tenantID uuid.UUID, now time.Time, mode string) {
	expectTenantConfigRead(mock, tenantID, now, mode, ageverification.None)
}

// expectSeriesRating stands in for the derived figure every series detail read
// carries. It is matched by the query's name rather than by a copy of its text:
// the query is one aggregate over the daily stats and the tally beside them,
// and no handler test turns on how it is written.
func expectSeriesRating(mock sqlmock.Sqlmock, tenantID, seriesID uuid.UUID, average float64, count int64) {
	mock.ExpectQuery("-- name: GetSeriesRating :one").
		WithArgs(tenantID, seriesID).
		WillReturnRows(sqlmock.NewRows([]string{"rating_count", "rating_average"}).AddRow(count, average))
}
