version: "2"
sql:
  - engine: postgresql
    schema: db/schema/
    queries: db/query/
    gen:
      go:
        package: dbmodels
        out: server/internal/db
        emit_json_tags: true
        emit_interface: true
        emit_exact_table_names: false
