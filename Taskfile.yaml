version: "3"

includes:
  db:
    taskfile: ./db/Tafkfile.yaml
    dir: ./db
  server:
    taskfile: ./server/Taskfile.yaml
    dir: ./server

tasks:
  setup:
    cmds:
      - pnpm install
      - task: server:tidy

  gen:
    cmds:
      - sqlc generate
      - buf generate
