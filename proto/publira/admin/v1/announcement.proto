syntax = "proto3";

package publira.admin.v1;

option go_package = "github.com/publira/publira/server/gen/publira/admin/v1;publiraadminv1";

import "publira/types/v1/types.proto";

enum AnnouncementAudienceType {
  ANNOUNCEMENT_AUDIENCE_TYPE_UNSPECIFIED = 0;
  ANNOUNCEMENT_AUDIENCE_TYPE_ALL_USERS = 1;
  ANNOUNCEMENT_AUDIENCE_TYPE_SELECTED_USERS = 2;
}

message AdminAnnouncement {
  string id = 1;
  string title = 2;
  string body = 3;
  string link_url = 4;
  AnnouncementAudienceType audience_type = 5;
  string target_user_public_id = 6;
  string target_user_name = 7;
  string created_at = 8;
}

// Cursor pagination. Field shape and token rules: proto/README.md.
message ListAnnouncementsRequest {
  // Removed with the move to cursor pagination.
  reserved 3;
  reserved "offset";

  publira.types.v1.TenantContext tenant = 1;
  // Max items in one page. <= 0 or > 100 falls back to 20.
  int32 limit = 2;
  // Opaque token from a previous response. Empty for the first page.
  string token = 4;
}

message ListAnnouncementsResponse {
  repeated AdminAnnouncement announcements = 1;
  // Token for the previous page. Empty on the first page.
  string previous_token = 2;
  // Token for the next page. Empty on the last page.
  string next_token = 3;
}

message CreateAnnouncementRequest {
  publira.types.v1.TenantContext tenant = 1;
  string title = 2;
  string body = 3;
  string link_url = 4;
  AnnouncementAudienceType audience_type = 5;
  repeated string target_user_public_ids = 6;
}

message CreateAnnouncementResponse {
  repeated AdminAnnouncement announcements = 1;
}

service AdminAnnouncementService {
  rpc ListAnnouncements(ListAnnouncementsRequest) returns (ListAnnouncementsResponse) {}
  rpc CreateAnnouncement(CreateAnnouncementRequest) returns (CreateAnnouncementResponse) {}
}
