/// The `ClientSurface` every catalog read of the app names, so the API answers
/// with the works the tenant shows in the app rather than on the storefront.
const appClientSurface = 'CLIENT_SURFACE_APP';
