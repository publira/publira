/// The `ClientSurface` the app names on every catalog read and on every call
/// of the reader's own that acts on a work — follows, reading positions and
/// history, ratings, and comments — so the API answers with the works the
/// tenant shows in the app rather than on the storefront.
const appClientSurface = 'CLIENT_SURFACE_APP';
